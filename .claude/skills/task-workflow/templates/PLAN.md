# PLAN.md: <название>

**Последнее обновление:** <дата>

---

## Scope (резюме из SPEC.md)

- **Модули:** <список пакетов/модулей>
- **Новые файлы:**
  - `<путь>` — <краткое назначение>
- **Изменяемые файлы:**
  - `<путь>`
- **Breaking changes:** нет | да (<описание>)
- **Коммиты:** C1 (всё вместе) | C1 + C2 (если нужна разбивка) — с обоснованием разбивки

## Контекст

### Текущее состояние
<что есть сейчас в кодобазе, как это работает>

### Решение (1-2 фразы)
<как решаем, короткая формулировка — согласовано с SPEC.md>

### Паттерн-референс (опционально)
<если есть аналогичная реализация в проекте — ссылка на файл/функцию, которой следуем>

## Bricks (шаги для worker'ов)

Каждая brick — единица работы, которую можно отдать одному worker subagent'у.

### Brick 1: <короткое название>

- **Файл:** `<путь>`
- **Действие:** create | update | delete
- **TDD sequence:**
  - [ ] RED tests: <какие, в каком файле>
  - [ ] Implementation
  - [ ] Run targeted checks: `<команда>`
- **Worker assignment:** prev-tier coding model
- **Analyzer checkpoint:** plan-techlead / plan-architect / plan-simplifier / none — кто ревьюит diff
- **Детали:**
  - <конкретное изменение>
  - <конкретное изменение>
- **Expected output (что worker возвращает):** changed files, evidence из commands, findings/blockers

### Brick 2: <короткое название>

<...>

<продолжай bricks по файлам / по поверхностям>

## Что НЕ делаем (осознанно)

- <явный отказ с причиной>
- <явный отказ с причиной>

## Проверка

- **Сборка:** `<команда>`
- **Линтеры:** `<команда>`
- **Тесты:** `<команда>`
- **Race detector:** `<команда>`
- **Поведенческий чеклист (из SPEC.md acceptance criteria):**
  - [ ] <критерий 1>
  - [ ] <критерий 2>
  - [ ] <регрессия: ничего не сломалось>

## Manual gaps

<acceptance criteria из SPEC.md, которые НЕ покрыты автоматическими тестами — что и как проверим руками>

- <критерий>: <как проверим>

## Риски

- **<формулировка>:** <описание риска + митигация>
- **<формулировка>:** <описание риска + митигация>

## Rollback / compatibility

<если изменение трогает контракты / миграции / релиз-путь — как откатим, как обеспечиваем обратную совместимость>
